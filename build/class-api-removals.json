{
  "autocomplete-buttons": "The adornment buttons are children of the .form-control-group itself now, laid out by the group, so the wrapper is gone. Use .form-control-action / .form-control-cleaner on the buttons.",
  "autocomplete-cleaner": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "autocomplete-dropdown": "The generated options dropdown is the shared combobox surface now (.popup + .combobox-popup), so the component never emits this class - see \"Autocomplete and Multi Select share one options menu\" in the migration guide. The last selectors carrying it only fed tokens to a teleported dropdown that had stopped existing.",
  "autocomplete-indicator": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "autocomplete-input": "The editable inside a field component is a plain .form-control now — the group strips its frame, so it needs no class of its own. See .form-control-group in the v6 migration guide.",
  "autocomplete-input-group": "The frame of every field component is the shared .form-control-group primitive now, so the input group squares off that one class instead of a different wrapper per family. Nothing has emitted these since the primitive landed - the only thing keeping them in the stylesheet was the input-group rules that matched them, which matched no markup.",
  "autocomplete-lg": "The frame is the size surface now: put .form-control-sm / .form-control-lg on the .form-control-group. The family-named class declared its tokens on the component root, where the frame's own declarations shadowed them - it never had an effect in v6.",
  "autocomplete-optgroup-label": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "autocomplete-option": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "autocomplete-options": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "autocomplete-options-empty": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "autocomplete-sm": "The frame is the size surface now: put .form-control-sm / .form-control-lg on the .form-control-group. The family-named class declared its tokens on the component root, where the frame's own declarations shadowed them - it never had an effect in v6.",
  "bg-*-gradient": "Removed in v6 — a CoreUI-only addition. Use the base colour plus Bootstrap's generic gradient: class=\"bg-primary bg-gradient\".",
  "calendar-nav-icon-*": "Removed in v6 — the calendar navigation icons render in JavaScript now. Customize them with the navIconPrev / navIconNext / navIconDoublePrev / navIconDoubleNext options.",
  "dark\\:bg-black": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-body": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-body-secondary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-body-tertiary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-danger": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-danger-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-dark": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-dark-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-info": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-info-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-light": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-light-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-primary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-primary-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-secondary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-secondary-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-success": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-success-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-transparent": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-warning": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-warning-subtle": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:bg-white": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-*emphasis*": "Removed in v6 with the emphasis utilities they varied — see .text-high-emphasis and friends above.",
  "dark\\:text-black": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-black-50": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-body": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-body-secondary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-body-tertiary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-danger": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-dark": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-disabled": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-disabled-inverse": "Removed in v6 with the emphasis utilities — see .text-disabled-inverse above.",
  "dark\\:text-info": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-light": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-muted": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-primary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-reset": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-secondary": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-success": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-warning": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-white": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "dark\\:text-white-50": "The dark:/light: theme-variant utilities are gone. They were generated only for the colour utilities, never documented, and duplicated every value under a [data-coreui-theme] selector. Use the colour-mode-aware tokens instead - the palette already resolves per theme through light-dark(), so .bg-body follows the theme without a variant class.",
  "date-picker-cleaner": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "date-picker-dropdown": "Renamed to .date-picker-popup / .time-picker-popup. The panel is the shared `.popup` surface, and every other family had already dropped the `-dropdown` vocabulary when the options menu became .combobox-popup; the pickers were the last holdout. The class carries the panel's tokens now instead of inheriting them from the field's root, so it works the same teleported, portaled or in place.",
  "date-picker-indicator": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "date-picker-input": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "date-picker-input-group": "The frame of every field component is the shared .form-control-group primitive now, so the input group squares off that one class instead of a different wrapper per family. Nothing has emitted these since the primitive landed - the only thing keeping them in the stylesheet was the input-group rules that matched them, which matched no markup.",
  "date-picker-input-preview": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "date-picker-input-wrapper": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "date-picker-lg": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "date-picker-separator": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "date-picker-sm": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "elevation-*": "Removed in v6 with the elevation system, which was disabled by default ($enable-elevations: false). Use the shadow utilities: .shadow, .shadow-sm, .shadow-lg.",
  "form-check-reverse": "Removed in v6. It right-flushed the label and the control as a pair, which the order utilities express directly, and it was never the settings row (label on the left, control on the right) it is usually reached for - that one is justify-content: space-between. See the v6 migration guide.",
  "form-multi-select-all": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-all-with-checkbox": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-buttons": "The adornment buttons are children of the .form-control-group itself now, laid out by the group, so the wrapper is gone. Use .form-control-action / .form-control-cleaner on the buttons.",
  "form-multi-select-cleaner": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "form-multi-select-dropdown": "The generated options dropdown is the shared combobox surface now (.popup + .combobox-popup), so the component never emits this class - see \"Autocomplete and Multi Select share one options menu\" in the migration guide. The last selectors carrying it only fed tokens to a teleported dropdown that had stopped existing.",
  "form-multi-select-dropdown-header": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-indeterminate": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-indicator": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "form-multi-select-input-group": "The frame of every field component is the shared .form-control-group primitive now, so the input group squares off that one class instead of a different wrapper per family. Nothing has emitted these since the primitive landed - the only thing keeping them in the stylesheet was the input-group rules that matched them, which matched no markup.",
  "form-multi-select-lg": "The frame is the size surface now: put .form-control-sm / .form-control-lg on the .form-control-group. The family-named class declared its tokens on the component root, where the frame's own declarations shadowed them - it never had an effect in v6.",
  "form-multi-select-optgroup": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-optgroup-label": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-optgroup-label-with-checkbox": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-option": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-option-with-checkbox": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-options": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-options-empty": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-multi-select-sm": "The frame is the size surface now: put .form-control-sm / .form-control-lg on the .form-control-group. The family-named class declared its tokens on the component root, where the frame's own declarations shadowed them - it never had an effect in v6.",
  "form-multi-select-tag": "Multi Select renders every selection as a Chip now, so `selectionType: 'tags'` and `'chips'` produce the same markup — use .chip and .chip-remove. The tag build was a private copy of the chip, without the roles chips carry.",
  "form-multi-select-tag-delete": "Multi Select renders every selection as a Chip now, so `selectionType: 'tags'` and `'chips'` produce the same markup — use .chip and .chip-remove. The tag build was a private copy of the chip, without the roles chips carry.",
  "form-multi-selected": "The generated options dropdown of Autocomplete and Multi Select is one shared surface now — use the unified combobox-* classes (combobox-popup/-header/-all/-options/-option/-optgroup and the selected/indeterminate states).",
  "form-password": "The frame of every field component is the shared .form-control-group primitive now, so the input group squares off that one class instead of a different wrapper per family. Nothing has emitted these since the primitive landed - the only thing keeping them in the stylesheet was the input-group rules that matched them, which matched no markup.",
  "form-password-action": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "form-password-action-icon": "The password toggle's icon is rendered by the plugin now, as inline SVG on currentColor like every other adornment in the library, so the button needs no icon markup: write an empty <button class=\"form-control-action\" data-coreui-toggle=\"password\">. Pass your own artwork through the showIcon / hideIcon options instead of overriding $form-password-icon-* - those variables are gone with the class, and with them 2.5 kB of encoded SVG that every stylesheet carried whether or not the page had a password field.",
  "hover": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "modal-backdrop": "The backdrop is the native ::backdrop pseudo-element now - no element is inserted into the DOM. Style it through the --cui-modal-backdrop-bg / --cui-modal-backdrop-opacity tokens on .modal.",
  "modal-content": "Modal renders on the native <dialog> element now: the positioning shell and the content box are the single <dialog class=\"modal\"> element, so the wrapper layers are gone. Size and fullscreen modifiers sit on the dialog itself. Legacy v5 markup is auto-upgraded at runtime (with a console warning) until v7 - see \"Modal and Offcanvas on the native dialog\" in the migration guide.",
  "modal-dialog": "Modal renders on the native <dialog> element now: the positioning shell and the content box are the single <dialog class=\"modal\"> element, so the wrapper layers are gone. Size and fullscreen modifiers sit on the dialog itself. Legacy v5 markup is auto-upgraded at runtime (with a console warning) until v7 - see \"Modal and Offcanvas on the native dialog\" in the migration guide.",
  "modal-dialog-centered": "The browser centers a native modal <dialog> in the viewport, so centering needs no class - it is the default and only behavior.",
  "modal-dialog-scrollable": "Renamed to .modal-scrollable, placed on the <dialog class=\"modal\"> itself now that the wrapper layers are gone.",
  "offcanvas-backdrop": "The backdrop is the native ::backdrop pseudo-element now - no element is inserted into the DOM. Style it through the --cui-offcanvas-backdrop-bg / --cui-offcanvas-backdrop-opacity tokens on .offcanvas.",
  "showing": "Offcanvas state classes are replaced by the native <dialog> [open] attribute; the exit transition still uses .hiding while the JS keeps the dialog in the top layer. There is no entry counterpart to migrate to - [open] is set before the transition starts.",
  "text-bg-*-gradient": "Removed in v6 — a CoreUI-only addition. Use class=\"text-bg-primary bg-gradient\".",
  "text-disabled-inverse": "Removed in v6 — a CoreUI-only addition, deprecated since v5.0.0. Use .text-white with an opacity utility.",
  "text-high-emphasis": "Removed in v6 — a CoreUI-only addition, deprecated since v5.0.0. Use .text-body.",
  "text-high-emphasis-inverse": "Removed in v6 — a CoreUI-only addition, deprecated since v5.0.0. Use .text-white with an opacity utility.",
  "text-medium-emphasis": "Removed in v6 — a CoreUI-only addition, deprecated since v5.0.0. Use .text-body-secondary.",
  "text-medium-emphasis-inverse": "Removed in v6 — a CoreUI-only addition, deprecated since v5.0.0. Use .text-white with an opacity utility.",
  "time-picker-cleaner": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "time-picker-dropdown": "Renamed to .date-picker-popup / .time-picker-popup. The panel is the shared `.popup` surface, and every other family had already dropped the `-dropdown` vocabulary when the options menu became .combobox-popup; the pickers were the last holdout. The class carries the panel's tokens now instead of inheriting them from the field's root, so it works the same teleported, portaled or in place.",
  "time-picker-indicator": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "time-picker-inline-icon": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "time-picker-input": "The v1 picker shells are gone: the pickers are now composed from the section-input field, Calendar and TimeSelection, so the field internals are styled by forms/_form-date-time.scss and the icons are inline SVG rendered by the component. See the v6 migration guide.",
  "time-picker-input-group": "The frame of every field component is the shared .form-control-group primitive now, so the input group squares off that one class instead of a different wrapper per family. Nothing has emitted these since the primitive landed - the only thing keeping them in the stylesheet was the input-group rules that matched them, which matched no markup.",
  "time-picker-lg": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide.",
  "time-picker-sm": "The frame of every field component is the shared .form-control-group primitive now. Use .form-control-action for an adornment button, .form-control-cleaner for the one that clears the value, .form-control-icon for decoration, and .form-control-sm / .form-control-lg on the group for sizing. See the v6 migration guide."
}
