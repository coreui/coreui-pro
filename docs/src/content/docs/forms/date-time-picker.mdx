---
pro: true
title: "Bootstrap 5 Date Time Picker"
name: "Date Time Picker"
description: "The v6 date time picker: a masked date-time field composed with a calendar and the time selection body. Replaces the timepicker flag of the v1 date picker."
---

<Callout color="warning">
  **Preview.** A new v6 product: it replaces `data-coreui-timepicker="true"` on
  the v1 [date picker](/forms/date-picker/). Composed from the
  [date time input](/forms/date-time-input/) masked field, the
  [calendar](/components/calendar/), and the shared time selection body. The API
  is under review and **will change**. See the
  [v6 migration guide](/migration/v6/).
</Callout>

## Example

The popup shows the calendar and the time selection side by side. The two halves
own different parts of one value — picking a day keeps the time, picking a time
keeps the day — so the popup does not auto-close.

<Example pro code={`<div class="row">
    <div class="col-lg-6 mb-3">
      <div data-coreui-locale="en-US" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6">
      <div
        data-coreui-date="2026/03/15 14:30:00"
        data-coreui-locale="en-US"
        data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

### Variants

<Example pro code={`<div class="row">
    <div class="col-lg-6">
      <div data-coreui-locale="en-US" data-coreui-variant="select" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

### With footer

<Example pro code={`<div class="row">
    <div class="col-lg-6">
      <div data-coreui-locale="en-US" data-coreui-toggle="date-time-picker">
        <template data-coreui-template="footer">
          <button type="button" class="btn btn-sm btn-ghost-danger me-auto" data-coreui-picker-action="today">Today</button>
          <button type="button" class="btn btn-sm btn-ghost-primary" data-coreui-picker-action="clear">Clear</button>
          <button type="button" class="btn btn-sm btn-primary" data-coreui-picker-action="close">OK</button>
        </template>
      </div>
    </div>
  </div>`} />

## Sizing

<Example pro code={`<div class="row mb-3">
    <div class="col-lg-6">
      <div data-coreui-locale="en-US" data-coreui-size="lg" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6">
      <div data-coreui-locale="en-US" data-coreui-size="sm" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

## Disabled

<Example pro code={`<div class="row">
    <div class="col-lg-6">
      <div data-coreui-disabled="true" data-coreui-locale="en-US" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

## Without seconds

<Example pro code={`<div class="row">
    <div class="col-lg-6">
      <div data-coreui-locale="en-US" data-coreui-seconds="false" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

## Non-english locale

<Example pro code={`<div class="row mb-3">
    <div class="col-lg-6">
      <div data-coreui-locale="ja" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6">
      <div data-coreui-locale="ko" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

## Right to left support

<Example pro code={`<div class="row" dir="rtl">
    <div class="col-lg-6">
      <div data-coreui-locale="he-IL" data-coreui-toggle="date-time-picker"></div>
    </div>
  </div>`} />

## Usage

### Via data attributes

```html
<div data-coreui-toggle="date-time-picker"></div>
```

### Via JavaScript

```js
const picker = new coreui.DateTimePicker(element, { locale: 'en-US' })
```

### Options

Options belonging to the inner [calendar](/components/calendar/#options) or
[date time input](/forms/date-time-input/#options) are forwarded by name.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `allowList` | object | `SVGAllowlist` | Allowlist used when sanitizing the icon markup. |
| `ariaToggleLabel` | string | `'Toggle the calendar'` | Accessible label of the indicator button. |
| `calendarOptions` | object | `{}` | Programmatic escape hatch for calendar options. |
| `container` | string, element, false | `false` | Teleport target for the dropdown. |
| `date` | date, string, null | `null` | Initial value (date **and** time). |
| `disabled` | boolean | `false` | Disables the field and the popup. |
| `hours` / `minutes` / `seconds` | array, boolean, function | — | Allowed time values, forwarded to the selection body. |
| `indicatorIcon` | string | calendar SVG | Inline SVG rendered by the component (sanitized). |
| `inputOptions` | object | `{}` | Programmatic escape hatch for field options. |
| `locale` | string | browser locale | Drives the mask, the calendar labels, and the 12/24-hour split. |
| `maxDate` / `minDate` | date, string, null | `null` | Bounds, enforced by both layers. |
| `name` | string, null | `null` | Name of the generated hidden input. |
| `selectionOptions` | object | `{}` | Programmatic escape hatch for the selection body. |
| `size` | string, null | `null` | `'sm'` or `'lg'`. |
| `variant` | string | `'roll'` | Time selection variant: `'roll'` or `'select'`. |

### Methods

| Method | Description |
| --- | --- |
| `show` / `hide` / `toggle` | Popup control. |
| `getDate` | Returns the current value (`Date` or `null`). |
| `setDate` | Sets the value and emits `dateChange`. |
| `today` | Sets the current date and time. |
| `clear` | Clears the value. |
| `reset` | Restores the initial value. |
| `getContext` | Returns the slot context (same shape as the date picker). |
| `dispose` | Destroys the picker and its inner components. |

### Events

| Event | Description |
| --- | --- |
| `dateChange.coreui.date-time-picker` | Fired when either half changes the value. `event.date` carries the merged `Date`. |
| `show/shown/hide/hidden.coreui.date-time-picker` | Popup lifecycle. |

## Customizing

### CSS variables

<ScssDocs file="scss/_date-picker.scss" capture="date-picker-css-vars" />

### SASS variables

<ScssDocs file="scss/_date-picker.scss" capture="date-picker-variables" />

<ScssDocs file="scss/_config.scss" capture="time-picker-variables" />
