---
pro: true
title: "Bootstrap 5 Date Picker"
name: "Date Picker"
description: "The v6 date picker: a section-based masked field composed with a calendar popup. Projected footer regions replace button configuration props."
---

import datePickerFormats from './snippets/date-picker-formats.js?raw'
import datePickerDisabledDates from './snippets/date-picker-disabled-dates.js?raw'
import datePickerFooterToday from './snippets/date-picker-footer-today.js?raw'
import datePickerWeekends from './snippets/date-picker-weekends.js?raw'

<Callout color="warning">
  **Preview.** This is the in-progress v6 rewrite of the date picker — a
  composition of the [date input](/forms/date-input/) masked field, the
  [calendar](/components/calendar/), and a shared popup primitive. The API is
  under review and **will change**; the current [date picker](/forms/date-picker/)
  remains the stable component. See the [v6 migration guide](/migration/v6/) for
  the option-by-option map.
</Callout>

## Example

Below is an example of a basic Date Picker.

### Days

The input is a section-based mask — type the date directly, adjust a section
with the arrow keys, or pick a day from the calendar. There is no free-text
parsing and no debounce: the field always holds a valid partial date.

<Example pro code={`<div class="row">
    <div class="col-sm-6 col-lg-5 mb-3 mb-sm-0">
      <div data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
    <div class="col-sm-6 col-lg-5">
      <div data-coreui-date="2026/03/15" data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

#### With footer

There is no `footer` option. Project a `<template data-coreui-template="footer">`
child instead — its content is cloned into the dropdown. Buttons act through
`data-coreui-picker-action` (`today`, `clear`, `reset`, `close`), so labels,
order, and translations are yours. A button opting into the `today` action
opts into its state too: it is disabled at initialization when today falls
outside `minDate`/`maxDate`/`disabledDates`. This is one-way — the picker only
ever sets `disabled`, so a button you disable in the template stays disabled.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-locale="en-US" data-coreui-toggle="date-picker">
        <template data-coreui-template="footer">
          <button type="button" class="btn btn-sm btn-ghost-danger me-auto" data-coreui-picker-action="today">Today</button>
          <button type="button" class="btn btn-sm btn-ghost-primary" data-coreui-picker-action="clear">Clear</button>
          <button type="button" class="btn btn-sm btn-primary" data-coreui-picker-action="close">OK</button>
        </template>
      </div>
    </div>
  </div>`} />

Here `maxDate` is the end of last month, so the `today` button disables
itself. The state of any other button is yours — the slot context exposes
`isDateSelectable(date)`, which answers whether a date, expressed in the
field's mask, passes the `minDate`/`maxDate`/`disabledDates` validation (a day
mask checks the calendar day, so `new Date()` passes with a `maxDate` of
today) — and every programmatic date still runs through the field's
validation: out of range, the field turns `is-invalid`, `errorChange` reports
the reason, and `getDate()`/`dateChange` report `null`, the same outcome as
typing that date.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div id="myDatePickerFooterToday">
        <template data-coreui-template="footer">
          <button type="button" class="btn btn-sm btn-ghost-danger me-auto" data-coreui-picker-action="today">Today</button>
          <button type="button" class="btn btn-sm btn-primary" data-coreui-picker-action="close">OK</button>
        </template>
      </div>
    </div>
  </div>`} sandboxJs={datePickerFooterToday} />

<Code code={datePickerFooterToday} lang="js" />

### Weeks

Add `data-coreui-selection-type="week"` to select week numbers, and
`data-coreui-show-week-number="true"` to show them in the grid. The field
renders the native `<input type="week">` presentation — a fixed, non-editable
localized label followed by the week and year sections (`Week 29, 2026`,
`Tydzień 29, 2026`, …). The year is the ISO week-numbering year, which can
differ from the calendar year around January 1st, and the reported `Date` is
the Monday of the selected week. Prefer the ISO wire format in the field?
Set `data-coreui-format="yyyy-Www"`.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div
        data-coreui-locale="en-US"
        data-coreui-selection-type="week"
        data-coreui-show-week-number="true"
        data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Months

Add `data-coreui-selection-type="month"` to select months. The field mask
narrows to month and year automatically.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div
        data-coreui-locale="en-US"
        data-coreui-selection-type="month"
        data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Quarters

Add `data-coreui-selection-type="quarter"` to select quarters — the equivalent
of the `<input type="quarter">` that HTML never shipped. The field mask narrows
to a quarter and year section (`Q4 2026`), and the reported `Date` is the
first day of the selected quarter.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div
        data-coreui-locale="en-US"
        data-coreui-selection-type="quarter"
        data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Years

Add `data-coreui-selection-type="year"` to select years. The field mask narrows
to the year automatically.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div
        data-coreui-locale="en-US"
        data-coreui-selection-type="year"
        data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

## Sizing

Set heights using the `data-coreui-size` attribute like `data-coreui-size="lg"`
and `data-coreui-size="sm"`.

<Example pro code={`<div class="row mb-3">
    <div class="col-lg-5">
      <div data-coreui-locale="en-US" data-coreui-size="lg" data-coreui-toggle="date-picker"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-5">
      <div data-coreui-locale="en-US" data-coreui-size="sm" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

## Disabled

Add the `data-coreui-disabled="true"` attribute to give the picker a grayed out
appearance and remove pointer events.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-disabled="true" data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

<Callout color="info">
  **`inputReadOnly` and `previewDateOnHover` are gone.** A section field is
  never free-text, so there is nothing to make read-only — use `disabled`. The
  hover preview wrote into the input value, which does not compose with
  sections. See the [migration guide](/migration/v6/).
</Callout>

## Disabled dates

The `disabledDates` option excludes specific dates — single `Date`s,
`[start, end]` ranges, or a matcher function. The calendar grays them out and
the field refuses them: a completed date that hits a disabled entry gets the
`.is-invalid` class and the picker reports `null` until it is corrected.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div id="myDatePickerDisabledDates"></div>
    </div>
  </div>`} sandboxJs={datePickerDisabledDates} />

<Code code={datePickerDisabledDates} lang="js" />

### Disabling weekends

Pass a function to disable every date that matches a rule.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div id="myDatePickerWeekends"></div>
    </div>
  </div>`} sandboxJs={datePickerWeekends} />

<Code code={datePickerWeekends} lang="js" />

## Non-english locale

### Auto

The locale drives both layers: the section order and separators of the field,
and the month/weekday names of the calendar. By default it comes from the
browser.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Chinese

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-locale="zh-CN" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Japanese

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-locale="ja" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Korean

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-locale="ko" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

## Right to left support

RTL support is built-in and can be explicitly controlled through the
`$enable-rtl` variable in Sass. The popup flips its anchoring side with the
document direction.

### Hebrew

<Example pro code={`<div class="row" dir="rtl">
    <div class="col-lg-5">
      <div data-coreui-locale="he-IL" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

### Persian

<Example pro code={`<div class="row" dir="rtl">
    <div class="col-lg-5">
      <div data-coreui-locale="fa-IR" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

## Custom formats

`inputDateFormat` and `inputOnChangeDelay` are gone — the mask itself defines
the format. Set it with `data-coreui-format` (or the `format` option), which the
field forwards from the shell. The parser accepts both dayjs/moment-style tokens
(`DD`, `MM`, `YYYY`, `YY`) and date-fns/Unicode-style tokens (`dd`, `MM`,
`yyyy`, `yy`), plus `ww` for ISO week numbers and `q` (numeric) or `QQQ`
(`Q1`–`Q4`) for quarters; any other character becomes a literal separator.

<Example pro code={`<div class="row">
    <div class="col-sm-6 col-lg-5 mb-3 mb-sm-0">
      <label class="form-label">dd.MM.yyyy</label>
      <div data-coreui-format="dd.MM.yyyy" data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
    <div class="col-sm-6 col-lg-5">
      <label class="form-label">YYYY-MM-DD</label>
      <div data-coreui-format="YYYY-MM-DD" data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} />

A format does not have to include every section — a month-and-year mask reports
the first day of the month.

<Example pro code={`<div class="row">
    <div class="col-lg-5">
      <div data-coreui-format="MMMM yyyy" data-coreui-locale="en-US" data-coreui-toggle="date-picker"></div>
    </div>
  </div>`} sandboxJs={datePickerFormats} />

## Accessibility

The field is a [date input](/forms/date-input/#accessibility) — a group of spinbutton sections — so its own arrow keys belong to the value, and the popup opens on the native control's dropdown keys. Focus enters the calendar on the selected date, on today when nothing is selected, or on the last selectable date when today is out of range.

### Keyboard support

| Key | Action |
| --- | --- |
| <kbd>Alt</kbd> + <kbd>Arrow Down</kbd> / <kbd>F4</kbd> | Open the calendar popup and move focus into the grid |
| <kbd>Escape</kbd> | Close the popup and return focus to the field |
| <kbd>Tab</kbd> / <kbd>Shift</kbd> + <kbd>Tab</kbd> | Cycle between the field, its buttons, and the open popup |
| <kbd>Arrow Right</kbd> / <kbd>Arrow Left</kbd> | Move focus to the next / previous day; crossing the edge of the grid moves to the adjacent month |
| <kbd>Arrow Down</kbd> / <kbd>Arrow Up</kbd> | Move focus to the same day of the next / previous week |
| <kbd>Home</kbd> / <kbd>End</kbd> | Move focus to the first / last selectable day of the current week |
| <kbd>Page Down</kbd> / <kbd>Page Up</kbd> | Move focus to the same day of the next / previous month, keeping the day number when the target month is long enough |
| <kbd>Shift</kbd> + <kbd>Page Down</kbd> / <kbd>Shift</kbd> + <kbd>Page Up</kbd> | Move focus to the same day of the next / previous year |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Select the focused date and close the popup |

## Usage

### Via data attributes

Add `data-coreui-toggle="date-picker"` to a `div` element.

```html
<div data-coreui-toggle="date-picker"></div>
```

### Via JavaScript

```html
<div class="my-date-picker"></div>
```

```js
const datePickerElementList = Array.prototype.slice.call(document.querySelectorAll('.my-date-picker'))
const datePickerList = datePickerElementList.map(datePickerEl => {
  return new coreui.DatePicker(datePickerEl)
})
```

### Options

Options can be passed via data attributes or JavaScript. For data attributes,
append the option name to `data-coreui-`, converting "_camelCase_" to
"_kebab-case_" — write `data-coreui-selection-type="week"`, not
`data-coreui-selectionType="week"`.

Options belonging to the inner [calendar](/components/calendar/#options) or
[date input](/forms/date-input/#options) are forwarded by name, so their data
attributes work on the picker element too.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `allowList` | object | `SVGAllowlist` | Allowlist used when sanitizing the icon markup. |
| `ariaCleanerLabel` | string | `'Clear the value'` | Accessible label of the cleaner button. |
| `ariaToggleLabel` | string | `'Toggle the calendar'` | Accessible label of the indicator button. |
| `cleaner` | boolean | `true` | Renders a button that clears the value. It appears only once the field holds one. |
| `cleanerIcon` | string | cross SVG | Inline SVG rendered by the component (sanitized). |
| `calendarOptions` | object | `{}` | Programmatic escape hatch for calendar options. |
| `container` | string, element, false | `false` | Teleport target for the dropdown. |
| `date` | date, string, null | `null` | Initial value. |
| `disabled` | boolean | `false` | Disables the field and the popup. |
| `indicatorIcon` | string | calendar SVG | Inline SVG rendered by the component (sanitized). |
| `inputOptions` | object | `{}` | Programmatic escape hatch for date input options. |
| `locale` | string | browser locale | Drives section order, separators, and calendar labels. |
| `maxDate` | date, string, null | `null` | Upper bound, enforced by both layers. |
| `minDate` | date, string, null | `null` | Lower bound, enforced by both layers. |
| `name` | string, null | `null` | Name of the generated hidden input. |
| `sanitize` | boolean | `true` | Sanitize the icon markup. |
| `sanitizeFn` | function, null | `null` | Custom sanitizer. |
| `size` | string, null | `null` | `'sm'` or `'lg'`. |

### Methods

| Method | Description |
| --- | --- |
| `show` | Opens the popup. |
| `hide` | Closes the popup. |
| `toggle` | Toggles the popup. |
| `getDate` | Returns the current value (`Date` or `null`). |
| `setDate` | Sets the value and emits `dateChange`. |
| `today` | Sets today's date. |
| `clear` | Clears the value. |
| `reset` | Restores the initial value. |
| `getContext` | Returns the slot context (see below). |
| `dispose` | Destroys the picker and its inner components. |
| `getInstance` | Static — returns the instance for a DOM element. |
| `getOrCreateInstance` | Static — returns the instance, creating it if needed. |

#### Slot context

Projected regions (and any custom code) drive the picker through a small
imperative contract instead of configuration:

```js
const picker = coreui.DatePicker.getOrCreateInstance(element)
const context = picker.getContext()

context.today()          // set today's date
context.setDate(date)    // set any date
context.clear()          // clear the value
context.reset()          // restore the initial value
context.close()          // close the popup
context.date             // current value (Date | null)
context.disabled         // disabled state
```

### Events

| Event | Description |
| --- | --- |
| `dateChange.coreui.date-picker` | Fired when the value changes — from the calendar, the field, or a context action. `event.date` carries the new value. |
| `show.coreui.date-picker` | Fired immediately when `show` is called. |
| `shown.coreui.date-picker` | Fired when the popup has been made visible. |
| `hide.coreui.date-picker` | Fired immediately when `hide` is called. |
| `hidden.coreui.date-picker` | Fired when the popup has been hidden. |

The field's own events (`dateChange.coreui.date-input`,
`errorChange.coreui.date-input`) still bubble from the inner element — the shell
does not duplicate them.

```js
const myDatePicker = document.getElementById('myDatePicker')
myDatePicker.addEventListener('dateChange.coreui.date-picker', event => {
  console.log(event.date)
})
```

## Customizing

### CSS variables

The shell uses local CSS variables on `.date-picker` for real-time
customization. Values are set via Sass, so Sass customization is supported too.

<ScssDocs file="scss/_date-picker.scss" capture="date-picker-css-vars" />

<ScssDocs file="scss/_calendar.scss" capture="calendar-css-vars" />

### SASS variables

<DeprecatedIn version="6.0.0" />

Sass variables set these defaults at build time and are removed in v7. The CSS variables above reach the same values without recompiling, and can be set on a single element.

The shell reuses the existing `$date-picker-*` shell-layer variables — it adds
no new configuration surface. The input-layer variables (`$date-picker-input-*`,
`$date-picker-cleaner-*`, `$date-picker-separator-icon-*`) belong to the v1
shell and disappear with it.

<ScssDocs file="scss/_date-picker.scss" capture="date-picker-variables" />

<ScssDocs file="scss/_calendar.scss" capture="calendar-variables" />
