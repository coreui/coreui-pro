---
title: "Bootstrap 6 Number Input"
name: "Number Input"
description: "Add accessible increment and decrement buttons to a Bootstrap number field, with min, max and step handled by the browser."
---

# Number Input

A number field with a stepper you can actually hit. The browser's own spinner is
tiny, appears only on hover in some engines and does not exist at all on iOS, so
the component replaces it with two buttons sized like every other adornment in a
[`form-control-group`](/forms/form-control-group/).

## Example

<AddedIn version="6.0.0" />

Add `data-coreui-toggle="number-input"` to an `<input type="number">`. The
component wraps it in a [`form-control-group`](/forms/form-control-group/) and
adds the buttons — you write neither the frame, the buttons nor their icons.

<Example pro code={`<input type="number" class="form-control" value="3" aria-label="Quantity" data-coreui-toggle="number-input">`} />

## With form field

Wrap the control in [`.form-field`](/forms/field/) to lay it out with its label and description, and to get validation feedback in the right place.

<Example pro code={`<div class="form-field">
    <label for="numberInputField" class="form-label">Quantity</label>
    <input type="number" class="form-control" id="numberInputField" value="3" data-coreui-toggle="number-input">
    <div class="form-text">Up to ten items per order.</div>
  </div>`} />

## Min, max and step

`min`, `max` and `step` are the native attributes — the component does not
reimplement them, it calls the browser's own stepping. **A button that cannot
move the value any further is disabled**, so the bound is visible rather than
just unresponsive.

<Example pro code={`<input type="number" class="form-control" value="10" min="0" max="10" aria-label="Quantity" data-coreui-toggle="number-input">`} />

A fractional `step` works the same way:

<Example pro code={`<input type="number" class="form-control" value="2" step="0.5" aria-label="Amount" data-coreui-toggle="number-input">`} />

When the field is empty, the first step starts from `min` — or from zero if
there is no minimum.

## Sizing

Write `.form-control-sm` or `.form-control-lg` on the input as you would on any
other control, and the buttons follow.

Sizing belongs to the frame rather than to the control inside it, so the
component moves the class onto the group it builds — and with it **every other
class you wrote except `.form-control` itself**. Spacing, width and the rest
describe the field as a whole, and the field is now the frame; leaving `mb-3`
on the input would put the margin inside the border.

<Example pro code={`<input type="number" class="form-control form-control-sm mb-3" value="3" aria-label="Quantity" data-coreui-toggle="number-input">
  <input type="number" class="form-control form-control-lg" value="3" aria-label="Quantity" data-coreui-toggle="number-input">`} />

## Disabled

<Example pro code={`<input type="number" class="form-control" value="3" aria-label="Quantity" disabled data-coreui-toggle="number-input">`} />

## Inside your own group

An input that is already in a `.form-control-group` is left where you put it, so
a number field can share a frame with anything else the group holds:

<Example pro code={`<div class="form-control-group">
    <span class="form-control-icon">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" fill="currentColor"><path d="M464 240H272V48h-32v192H48v32h192v192h32V272h192z"/></svg>
    </span>
    <input type="number" class="form-control" value="3" aria-label="Quantity" data-coreui-toggle="number-input">
  </div>`} />

## Floating labels

The number input supports [floating labels](/forms/floating-labels/). Wrap the control in `.form-floating` and put a `<label>` beside it — the stepper buttons ride along with the field.

<Example pro code={`<div class="form-floating">
    <label for="floatingNumberInput">Quantity</label>
    <input class="form-control" type="number" placeholder=" " id="floatingNumberInput" value="42" data-coreui-toggle="number-input">
  </div>`} />

## Accessibility

The buttons carry labels (`Increase` / `Decrease` by default, both
configurable) but are **kept out of the tab order**: a number field already
steps with <kbd>↑</kbd> and <kbd>↓</kbd>, so putting them in would add two tab
stops per field for something the keyboard already reaches. Give the input an
`aria-label` or a `<label>` as you would any other field.

## Usage

Holding a button repeats after a short delay. Set `repeat: false` to require a
click per step.

```js
const numberInput = new coreui.NumberInput('#myNumberInput', {
  repeat: false
})

numberInput.increment()
numberInput.decrement()
```

### Options

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| `allowList` | object | `SVGAllowlist` | Allowlist the icons are sanitized against. |
| `ariaDecrementLabel` | string | `'Decrease'` | Accessible label for the decrement button. |
| `ariaIncrementLabel` | string | `'Increase'` | Accessible label for the increment button. |
| `decrementIcon` | string | minus | Icon markup for the decrement button. |
| `incrementIcon` | string | plus | Icon markup for the increment button. |
| `repeat` | boolean | `true` | Repeat the step while a button is held. |
| `sanitize` | boolean | `true` | Sanitize the icon markup. |
| `sanitizeFn` | function \| null | `null` | Your own sanitizer. |

### Events

| Event | Description |
| --- | --- |
| `change.coreui.number-input` | Fires after the value changes through a button, carrying the new `value`. |

The element also fires the native `input` and `change` events, so anything
already listening to the field keeps working.

<Callout color="info">
  **Decimal separators.** `<input type="number">` accepts the dot as the decimal
  separator regardless of locale, and a value the browser rejects is reported as
  an empty string — the field cannot tell you what was typed. If you need
  locale-aware separators, currency or grouping, that is a formatted-input
  problem rather than a stepper one, and `type="number"` is the wrong base for
  it.
</Callout>

## Customizing

### CSS variables

The frame, the buttons and their sizing are the group's, and read the shared
`--cui-control-*` properties:

<ScssDocs file="scss/forms/_form-control-group.scss" capture="form-control-group-tokens" />

### Sass variables

<DeprecatedIn version="6.0.0" />

Sass variables set these defaults at build time and are removed in v7. The CSS variables above reach the same values without recompiling, and can be set on a single element.

<ScssDocs file="scss/forms/_form-control-group.scss" capture="form-control-group-variables" />

### Sass maps

The group takes its own icon and action sizes per size class, and the map merges with your overrides — add a key to define a size, or set one to `null` to drop it:

<ScssDocs file="scss/forms/_form-control-group.scss" capture="form-control-group-sizes" />
