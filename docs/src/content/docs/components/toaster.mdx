---
title: "Bootstrap 6 Toaster"
name: "Toaster"
description: "Create toast notifications from JavaScript with the CoreUI Toaster: a store keyed by id with add, update, close and promise, a placed container, a visible-toasts limit and screen-reader priority."
otherFrameworks:
  - toaster
---

import toaster from './snippets/toaster.js?raw'
import toasterContent from './snippets/toaster-content.js?raw'
import toasterUpdate from './snippets/toaster-update.js?raw'
import toasterPromise from './snippets/toaster-promise.js?raw'
import toasterAction from './snippets/toaster-action.js?raw'
import toasterLimit from './snippets/toaster-limit.js?raw'
import toasterStack from './snippets/toaster-stack.js?raw'
import toasterPlacement from './snippets/toaster-placement.js?raw'
import toasterPlacementStack from './snippets/toaster-placement-stack.js?raw'

## Overview

<AddedIn version="6.0.0" /> The `Toaster` plugin creates [toasts](/components/toasts/) from options, so nothing has to be in the markup up front. It keeps a store of the toasts it shows, keyed by id, and one instance serves the whole page: `add()` returns the id, `update()` rewrites a toast in place, `close()` hides one or all of them, and `promise()` follows an asynchronous operation through its loading, success and error states.

```js
const toaster = new coreui.Toaster(null, { placement: 'top-end', limit: 3 })
const id = toaster.add({ title: 'Saved', description: 'Your changes are live.', theme: 'success' })
toaster.update(id, { description: 'Done' })
toaster.close(id)
```

Without an element the toaster creates its own `.toast-container` in `<body>`, placed by the `placement` option; pass an existing container instead to keep control over where the toasts land. The container is a `region` landmark named `Notifications`, so users of assistive technology can navigate to it.

## Examples

### Live example

Two toasters, each with its container in a top corner of the viewport: a list on the end side and a [stack](#stacking) on the start side. Every other example on this page passes a container that sits inside the example, so its toasts stay put.

<Example code={`<button type="button" class="btn-solid theme-primary" id="toasterLiveBtn">Add a toast</button>
  <button type="button" class="btn-solid theme-secondary" id="toasterLiveStackBtn">Add to the stack</button>`} sandboxJs={toaster} />

<Code code={toaster} lang="js" />

### Title, description and theme

A toast is a plain options object. `title` renders the header with a close button, `description` the body; a toast without a title puts the close button in the body. `theme` adds the `theme-{color}` class, `priority: 'high'` makes it an alert for screen readers, `timeout` overrides the toaster's default (`0` keeps the toast until it is closed) and `dismissible: false` drops the close button.

<Example code={`<div class="hstack gap-2 flex-wrap mb-3">
    <button type="button" class="btn-solid theme-primary" id="toasterTitleBtn">Title and description</button>
    <button type="button" class="btn-solid theme-secondary" id="toasterDescriptionBtn">Description only</button>
    <button type="button" class="btn-solid theme-danger" id="toasterThemeBtn">Themed, high priority</button>
    <button type="button" class="btn-outline theme-secondary" id="toasterStickyBtn">Not dismissible</button>
  </div>
  <div class="toast-container position-static" id="toasterContent"></div>`} sandboxJs={toasterContent} />

<Code code={toasterContent} lang="js" />

Strings are written as text. Set `html: true` on the toaster to render markup; it runs through the same sanitizer as tooltips, configurable with `allowList` and `sanitizeFn`.

### Updating a toast

Passing an `id` that already exists updates that toast instead of adding a second one, and bumps its `updateKey`, so a "Draft saved" toast clicked twice stays a single toast. `update()` does the same for a toast whose id you kept, and accepts a function of the current toast when the change depends on it: `toaster.update(id, previous => ({ description: previous.data.step + 1 }))`.

<Example code={`<div class="hstack gap-2 flex-wrap mb-3">
    <button type="button" class="btn-solid theme-primary" id="toasterDraftBtn">Save draft</button>
    <button type="button" class="btn-solid theme-secondary" id="toasterUploadBtn">Upload with progress</button>
  </div>
  <div class="toast-container position-static" id="toasterUpdate"></div>`} sandboxJs={toasterUpdate} />

<Code code={toasterUpdate} lang="js" />

### Promises

`promise()` adds a toast in the `loading` state and updates it when the promise settles: `success` receives the resolved value, `error` the rejection. Each state is a description string, a full options object, or a function returning either.

<Example code={`<button type="button" class="btn-solid theme-primary mb-3" id="toasterPromiseBtn">Save</button>
  <div class="toast-container position-static" id="toasterPromise"></div>`} sandboxJs={toasterPromise} />

<Code code={toasterPromise} lang="js" />

### Action

`action` renders a button in the body; its `onClick` receives the click event and the toast object, so an undo can rewrite the same toast.

<Example code={`<button type="button" class="btn-solid theme-primary mb-3" id="toasterArchiveBtn">Archive message</button>
  <div class="toast-container position-static" id="toasterAction"></div>`} sandboxJs={toasterAction} />

<Code code={toasterAction} lang="js" />

### Limit

`limit` caps how many toasts are visible at once. The newest stay on screen; older ones sink away from the edge and fade, remain in the DOM with `inert` and `data-coreui-limited`, and rise back in as the visible ones go away. `limit: 0` shows everything.

<Example code={`<div class="hstack gap-2 mb-3">
    <button type="button" class="btn-solid theme-primary" id="toasterLimitBtn">Add a toast</button>
    <button type="button" class="btn-outline theme-secondary" id="toasterLimitCloseBtn">Close all</button>
  </div>
  <div class="toast-container position-static" id="toasterLimit"></div>`} sandboxJs={toasterLimit} />

<Code code={toasterLimit} lang="js" />

### Stacking

With `stack: true` the toasts sit on top of each other: the newest in front at full size, the two behind it peeking out by `--cui-toast-stack-peek` and scaled down by `--cui-toast-stack-scale` per step, anything further back hidden. The offset of each step adds the height the scaling takes away, so every toast behind shows the same strip. Hovering or focusing the container spreads the stack into a list; leaving it folds the stack back. The stack takes the place of the flow layout, so `limit` still decides how many toasts exist, and the stack decides how many of them show — three by default (`--cui-toast-stack-visible`).

<Example code={`<div class="hstack gap-2 mb-3">
    <button type="button" class="btn-solid theme-primary" id="toasterStackBtn">Add a toast</button>
    <button type="button" class="btn-outline theme-secondary" id="toasterStackCloseBtn">Close all</button>
  </div>
  <div class="toast-container position-relative" id="toasterStack"></div>`} sandboxJs={toasterStack} />

<Code code={toasterStack} lang="js" />

The toaster keeps the geometry in tokens the CSS reads: `--cui-toast-stack-index`, `--cui-toast-stack-before` (the height of the toasts in front) and `--cui-toast-height` on each toast, `--cui-toast-stack-count`, `--cui-toast-stack-front-height` and `--cui-toast-stack-heights` on the container. Style the stack by these — the `data-coreui-stack-index` and `data-coreui-stack-hidden` attributes mark a toast's place in it.

A stack works in every placement: it grows away from the edge it is pinned to, so at the bottom the newest toast is the lowest one and the stack peeks upward.

<Example code={`<div class="d-grid gap-2" style="grid-template-columns: repeat(3, 1fr); max-width: 30rem" id="toasterPlacementStack">
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-start">Top start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-center">Top center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-end">Top end</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-start">Middle start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-center">Middle center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-end">Middle end</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-start">Bottom start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-center">Bottom center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-end">Bottom end</button>
  </div>`} sandboxJs={toasterPlacementStack} />

<Code code={toasterPlacementStack} lang="js" />

### Motion

A toast slides in from beyond the container's edge while it fades in — from the side in the `start` and `end` placements, from above or below in the `center` ones — and slides out sideways when it closes, so a toast closed in the middle of a list never has to pass through its neighbours. While a toast leaves, the ones behind it glide into the freed space; when a new one arrives, they make room the same way. `enter` picks another edge for the entry and `leave` the side for the exit — a `top-end` toaster can drop its toasts in from above and still send them out to the right — and a toast outside a placed container slides by `--cui-toast-slide` instead. All of it respects `prefers-reduced-motion`.

```js
const toaster = new coreui.Toaster(null, { placement: 'top-end', enter: 'top', leave: 'end' })
```

## Placement

`placement` pins the container the toaster creates to the viewport with one of the nine [`.toast-container-{block}-{inline}`](/components/toasts/#placement) classes: `top`, `middle` or `bottom` × `start`, `center` or `end`. In the `top` placements a new toast is inserted first, in the others last, so the newest always sits closest to the edge.

Each button below adds a toast through a toaster placed at that spot, so you can see where a toast enters from: from the side in the `start` and `end` columns, from above or below in the `center` column.

<Example code={`<div class="d-grid gap-2" style="grid-template-columns: repeat(3, 1fr); max-width: 30rem" id="toasterPlacement">
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-start">Top start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-center">Top center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="top-end">Top end</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-start">Middle start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-center">Middle center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="middle-end">Middle end</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-start">Bottom start</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-center">Bottom center</button>
    <button type="button" class="btn-outline theme-secondary" data-coreui-placement="bottom-end">Bottom end</button>
  </div>`} sandboxJs={toasterPlacement} />

<Code code={toasterPlacement} lang="js" />

A container you pass in is used as is — position it yourself:

```js
const inline = new coreui.Toaster('#notifications')
```

## Accessibility

The container is a `region` landmark named by `label` (`Notifications` by default), so screen-reader users can jump to it through landmark navigation. It holds two visually hidden live regions that exist before any toast does — a `role="status"` one that is polite and a `role="alert"` one that is assertive — and every added or updated toast writes its `title` and `description` into the one its `priority` selects. Only that text is announced, never the close button or the action, and the toast element itself carries no live role. `low`, the default, waits until the user is idle; keep `high` for the few messages that should interrupt, such as a failed save (WCAG 4.1.3 Status Messages).

Auto-hiding is a time limit on reading, which WCAG 2.2.1 Timing Adjustable asks you to let the user turn off, adjust or extend. Timers stop while the pointer or focus is inside the container (`pauseOnHover`), so a spread stack or a toast being read does not disappear mid-way, and the default `timeout` is the toaster's to set for the whole page. A toast with an action, or one the user must act on, should not hide on its own: give it `timeout: 0`, so keyboard and screen-reader users can reach the button. Toasts never take focus.

## Usage

Create a toaster once and keep the instance; every toast on the page goes through it.

```js
const toaster = new coreui.Toaster(null, { placement: 'bottom-end', limit: 3 })
const id = toaster.add({ title: 'Saved', description: 'Your changes are live.', theme: 'success' })
```


```js
const toaster = new coreui.Toaster(null, { placement: 'bottom-end', limit: 3 })
const id = toaster.add({ title: 'Saved', description: 'Your changes are live.', theme: 'success' })
```

### Options

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| `allowList` | object | [Default value](/getting-started/javascript#sanitizer) | Tags and attributes allowed in toast content when `html` is `true`. |
| `container` | string, element | `'body'` | Where the toaster appends the container it creates. Ignored when the toaster is given an existing element. |
| `enter` | string | `'auto'` | Edge a toast slides in from: `'start'`, `'end'`, `'top'` or `'bottom'`. `'auto'` follows the placement — the side in the `start` and `end` columns, above or below in the `center` one. |
| `html` | boolean | `false` | Render `title` and `description` as HTML instead of text. |
| `label` | string | `'Notifications'` | Accessible name of the container's `region` landmark. |
| `leave` | string | `'auto'` | Side a toast slides out to: `'start'` or `'end'`. A toast always leaves sideways, so it never has to pass through the toasts above or below it; `'auto'` is the side of the placement, `end` in the `center` column. |
| `limit` | number | `3` | How many toasts stay visible; the oldest beyond it are hidden and `inert` until a slot frees. `0` disables the limit. |
| `pauseOnHover` | boolean | `true` | Pause every toast's timer while the pointer or focus is inside the container, and resume when it leaves. Each toast still pauses on its own hover and focus. |
| `placement` | string | `'top-end'` | Corner or edge of the viewport for the container the toaster creates: `top`, `middle` or `bottom` × `start`, `center` or `end`. Toasts in the `top` placements are inserted first, in the others last, so the newest always sits closest to the edge. |
| `restartOnAdd` | boolean | `false` | Restart the timers of the visible toasts whenever a new one is added, so a burst of notifications leaves together. |
| `sanitize` | boolean | `true` | Sanitize HTML content. |
| `sanitizeFn` | null, function | `null` | Your own sanitizer, used instead of the built-in one. |
| `stack` | boolean | `false` | Stack the toasts on top of each other, newest in front, and spread them out on hover or focus. |
| `timeout` | number | `5000` | Default time (ms) before a toast hides. `0` keeps toasts until they are closed. |

### Toast options

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| `action` | object, null | — | `{ label, onClick(event, toast) }` renders a button in the body. |
| `class` | string | — | Extra classes on the `.toast`. |
| `data` | object | — | Anything you want to read back from the toast object later, e.g. in `update(id, previous => …)`. |
| `description` | string, element, function | — | Body of the toast. |
| `dismissible` | boolean | `true` | Render the close button. |
| `id` | string | generated | Stable id; adding with an existing id updates that toast in place. |
| `instant` | boolean | `false` | Skip the fade (`.toast-instant`). |
| `onClose` | function | — | Called when the toast starts hiding, with the toast object. |
| `onRemove` | function | — | Called after the toast left the DOM, with the toast object. |
| `priority` | string | `'low'` | Which live region announces the toast: `'low'` the polite `status` one, `'high'` the assertive `alert` one. |
| `theme` | string | — | Adds `theme-{color}`. |
| `timeout` | number | toaster's `timeout` | Time (ms) before this toast hides; `0` keeps it. |
| `title` | string, element, function | — | Header of the toast, with the close button next to it. |
| `translucent` | boolean | `false` | Adds `.toast-translucent`. |

### Methods

| Method | Description |
| --- | --- |
| `add(options)` | Renders and shows a toast. Returns its id. With an `id` that already exists it behaves like `update`. |
| `update(id, options)` | Rewrites the toast in place and increments its `updateKey`. `options` may be a function receiving the current toast object and returning the changes. |
| `close(id?)` | Hides the toast, which removes it from the DOM and the store once the fade ends. Without an id, hides every toast. |
| `promise(promise, { loading, success, error })` | Adds a toast in the `loading` state and updates it when the promise settles: `success` gets the resolved value, `error` the rejection. Each state is a description string or a full options object, or a function returning either. Returns the promise. |
| `pause()` / `resume()` | Stop and restart every toast's timer; `pauseOnHover` calls these for the container. |
| `getToasts()` | The toast objects in insertion order, each with `id`, `updateKey`, `limited`, `element` and the options it was created with. |
| `dispose` | Removes every toast, and the container if the toaster created it. |
| `getInstance` | *Static* method that returns the toaster bound to a container element. |
| `getOrCreateInstance` | *Static* method that returns the toaster bound to a container element or creates one on it. |

### Events

Events fire on the container. Each toast still fires the [toast events](/components/toasts/#events) on its own element.

| Event | Description |
| --- | --- |
| `add.coreui.toaster` | Fired after a toast was added; `event.id` is its id. |
| `remove.coreui.toaster` | Fired after a toast left the DOM; `event.id` is its id. |

## Customizing

### CSS variables

The toaster renders regular toasts, so their tokens apply — see the [toast CSS variables](/components/toasts/#css-variables). Three are its own: `--cui-toast-container-inset` on the container sets the distance from the viewport edge, `--cui-toast-translate` on the container is where a toast enters from and leaves to, overridden separately by `--cui-toast-enter-translate` and `--cui-toast-leave-translate` (the `enter` and `leave` options write those; set them yourself for any other offset), and `--cui-toast-limited-translate` on a toast sets where a toast over the limit sinks to.

<ScssDocs file="scss/_toasts.scss" capture="toast-container-css-vars" />

The stack's peek, scale step and visible count are tokens on `.toast-container-stack`:

<ScssDocs file="scss/_toasts.scss" capture="toast-stack" />

### Sass variables

<ScssDocs file="scss/_toasts.scss" capture="toast-variables" />
